<beans:beans xmlns="http://www.springframework.org/schema/security"
       xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:import resource="JpaConfig.xml"/>

    <!--<c:http security="none" pattern="/resources/**"/>-->
    <http use-expressions="true">
        <intercept-url pattern="/logout" access="permitAll"/>
        <intercept-url pattern="/auth" access="permitAll"/>
        <intercept-url pattern="/reg" access="permitAll"/>
        <intercept-url pattern="/news/**" access="permitAll"/>
        <logout
                logout-success-url="/"
                logout-url="/logout"
        />
        <form-login
                authentication-failure-url="/auth?error"
                login-page="/auth"
                login-processing-url="/auth"
                password-parameter="password"
                username-parameter="username"
        />
    </http>

    <beans:bean id="passwordEncoder"
          class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <beans:bean id="authorService" class="com.epam.newsportal.service.AuthorServiceImpl">
        <beans:property name="passwordEncoder" ref="passwordEncoder"/>
        <beans:property name="authorDao" ref="authorDao"/>
        <intercept-methods>
            <protect access="IS_AUTHENTICATED_ANONYMOUSLY" method="registration"/>
        </intercept-methods>
    </beans:bean>
    <beans:bean id="newsService" class="com.epam.newsportal.service.NewsServiceImpl">
        <beans:property name="newsDao" ref="newsDao"/>
        <intercept-methods>
            <protect access="IS_AUTHENTICATED_FULLY" method="*News"/>
        </intercept-methods>
    </beans:bean>

    <authentication-manager>
        <authentication-provider user-service-ref="authorService" >
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>